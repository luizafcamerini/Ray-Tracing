
class Camera():
    def __init__(angle: float, distance: float, ratio, eye, center, up: float):
        self.angle = angle
        self.distance = distance
        self.ratio = ratio
        self.eye = eye
        self.center = center
        self.up = up